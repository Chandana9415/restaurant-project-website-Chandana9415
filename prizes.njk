<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <title>Nobel Prizes in something</title>
  <link rel="stylesheet" href="prizes.css">
  <!-- Student edition, for homework purposes -->
</head>

<body>
  <main>
    <h1>Nobel Prizes in {{topic}}</h1>
      <h2>From {{start}} to {{end}} </h2>
      <nav>
          <p> Other categories: 
		  {% for x in arr1 %}
		  <a href="./{{x.fname}}">{{x.topic}}</a>
		  {% endfor %}
<!-- Links to other pages. Generate these from data passed in -->
          </p>
      </nav>
<table>
    <thead><tr>
        <th>Year</th> 
        <th>Name</th> 
        <th>Accomplishment</th>
        </tr></thead>
    <tbody>
		{% for y in arr2 %}
		<tr>
		{% for z in y.laureates %}
		<td> {{y.year}} </td>
		<td> {{z.firstname}}{{z.surname}} </td>
		<td> {{z.motivation}} </td>
		</tr>
		{% endfor %}
		{% endfor %}
<!-- Generate the table here. -->
    </tbody>
      </table>
  </main>
</body>

</html>
